<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/ProfileFragment">

    <fragment
        android:id="@+id/ProfileFragment"
        android:name="com.example.albumsapp.presentation.profile.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile">
        <action
            android:id="@+id/action_ProfileFragment_to_AlbumDetailsFragment"
            app:destination="@id/AlbumDetailsFragment" />
    </fragment>
    <fragment
        android:id="@+id/AlbumDetailsFragment"
        android:name="com.example.albumsapp.presentation.albumDetails.AlbumDetailsFragment"
        android:label="Album Details"
        tools:layout="@layout/fragment_album_details"/>
</navigation>